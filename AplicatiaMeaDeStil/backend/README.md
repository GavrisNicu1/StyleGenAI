# Deprecated

Acest folder `backend/` a fost folosit temporar pentru integrare. Nu mai este folosit.

Te rugăm folosește backend-ul tău separat (ex. proiectul `StyleGenAI`) și setează `BACKEND_URL` direct în `app/(tabs)/index.tsx`.

Poți șterge în siguranță folderul `backend/` din acest repo.
